# UI

Static HTML/JS. Serve `public/` with anything:

```
cd ui/public
python -m http.server 8080
```

Or deploy to Azure Static Web Apps / Storage `$web`.
Configure APIM URL + subscription key in the form (persisted in localStorage).
