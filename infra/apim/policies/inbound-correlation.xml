<!--
  Inbound correlation policy applied to the Voice Orders API.
  Enforces:
    - CORS (allow azurewebsites.net UI origin)
    - x-correlation-id (create if missing)
    - x-request-id from APIM context.RequestId (overwrite if missing)
    - traceparent / tracestate pass-through (W3C)
    - subscription-key header check (placeholder auth; skipped for OPTIONS preflight)
    - rate-limit (used in fault scenario 2)
-->
<policies>
  <inbound>
    <base />

    <!-- 0. CORS: allow browser calls from UI (App Service) -->
    <cors allow-credentials="true">
      <allowed-origins>
        <origin>https://aiosre-ui-demo.azurewebsites.net</origin>
        <origin>http://localhost:8080</origin>
        <origin>http://localhost:3000</origin>
      </allowed-origins>
      <allowed-methods preflight-result-max-age="300">
        <method>GET</method>
        <method>POST</method>
        <method>PUT</method>
        <method>DELETE</method>
        <method>OPTIONS</method>
      </allowed-methods>
      <allowed-headers>
        <header>*</header>
      </allowed-headers>
      <expose-headers>
        <header>x-correlation-id</header>
        <header>x-request-id</header>
        <header>x-trace-id</header>
      </expose-headers>
    </cors>

    <!-- Skip auth + heavy processing for CORS preflight -->
    <choose>
      <when condition="@(context.Request.Method == &quot;OPTIONS&quot;)">
        <return-response>
          <set-status code="200" reason="OK" />
        </return-response>
      </when>
    </choose>

    <!-- 1. Correlation ID (business txn) -->
    <set-variable name="cid" value="@(context.Request.Headers.GetValueOrDefault(&quot;x-correlation-id&quot;, Guid.NewGuid().ToString()))" />
    <set-header name="x-correlation-id" exists-action="override">
      <value>@((string)context.Variables["cid"])</value>
    </set-header>

    <!-- 2. Request ID = APIM RequestId -->
    <set-header name="x-request-id" exists-action="override">
      <value>@(context.RequestId.ToString())</value>
    </set-header>

    <!-- 3. W3C trace context pass-through (no-op if not present) -->
    <choose>
      <when condition="@(context.Request.Headers.ContainsKey(&quot;traceparent&quot;))">
        <set-header name="traceparent" exists-action="skip">
          <value>@(context.Request.Headers.GetValueOrDefault(&quot;traceparent&quot;,&quot;&quot;))</value>
        </set-header>
      </when>
    </choose>
    <choose>
      <when condition="@(context.Request.Headers.ContainsKey(&quot;tracestate&quot;))">
        <set-header name="tracestate" exists-action="skip">
          <value>@(context.Request.Headers.GetValueOrDefault(&quot;tracestate&quot;,&quot;&quot;))</value>
        </set-header>
      </when>
    </choose>

    <!-- 4. Subscription key required (placeholder auth) -->
    <check-header name="Ocp-Apim-Subscription-Key" failed-check-httpcode="401" failed-check-error-message="Missing subscription key" ignore-case="true" />

    <!-- 5. Rate limit: enabled by Scenario 2 fault.  Default 100/30s; lower it via APIM policy update during demo. -->
    <rate-limit-by-key calls="100" renewal-period="30" counter-key="@(context.Subscription?.Id ?? &quot;anon&quot;)" />
  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <base />
    <!-- Echo IDs back to the caller for UI visibility -->
    <set-header name="x-correlation-id" exists-action="override">
      <value>@((string)context.Variables["cid"])</value>
    </set-header>
    <set-header name="x-request-id" exists-action="override">
      <value>@(context.RequestId.ToString())</value>
    </set-header>
    <!-- Log to Event Hub → ADX (APIMGatewayLogs table) -->
    <log-to-eventhub logger-id="eh-diag-logger"><![CDATA[@{
      var body = new JObject(
        new JProperty("ts",             DateTime.UtcNow.ToString("o")),
        new JProperty("apiId",          context.Api.Id),
        new JProperty("apiName",        context.Api.Name),
        new JProperty("operationName",  context.Operation?.Name ?? ""),
        new JProperty("method",         context.Request.Method),
        new JProperty("url",            context.Request.OriginalUrl.ToString()),
        new JProperty("backendUrl",     context.Api.ServiceUrl.ToString()),
        new JProperty("requestId",      context.RequestId.ToString()),
        new JProperty("correlationId",  context.Variables.ContainsKey("cid") ? (string)context.Variables["cid"] : ""),
        new JProperty("traceparent",    context.Request.Headers.GetValueOrDefault("traceparent","")),
        new JProperty("subscriptionId", context.Subscription?.Id ?? ""),
        new JProperty("clientIp",       context.Request.IpAddress),
        new JProperty("status",         context.Response.StatusCode),
        new JProperty("totalTimeMs",    (DateTime.UtcNow - context.Timestamp).TotalMilliseconds)
      );
      return body.ToString(Newtonsoft.Json.Formatting.None);
    }]]></log-to-eventhub>
  </outbound>

  <on-error>
    <base />
    <log-to-eventhub logger-id="eh-diag-logger"><![CDATA[@{
      var body = new JObject(
        new JProperty("ts",            DateTime.UtcNow.ToString("o")),
        new JProperty("apiId",         context.Api.Id),
        new JProperty("operationName", context.Operation?.Name ?? ""),
        new JProperty("requestId",     context.RequestId.ToString()),
        new JProperty("correlationId", context.Variables.ContainsKey("cid") ? (string)context.Variables["cid"] : ""),
        new JProperty("traceparent",   context.Request.Headers.GetValueOrDefault("traceparent","")),
        new JProperty("status",        context.Response?.StatusCode ?? 0),
        new JProperty("errorReason",   context.LastError?.Reason ?? ""),
        new JProperty("errorMessage",  context.LastError?.Message ?? ""),
        new JProperty("totalTimeMs",   (DateTime.UtcNow - context.Timestamp).TotalMilliseconds)
      );
      return body.ToString(Newtonsoft.Json.Formatting.None);
    }]]></log-to-eventhub>
  </on-error>
</policies>
